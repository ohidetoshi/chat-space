.side-bar
  / ログインユーザー情報
  .side-bar__header
    %h3.side-bar__header__name
      = current_user.name
    %ul.side-bar__header__lists
      %li.list
        = link_to do
          = fa_icon 'pencil-square-o', class: 'icon'
      %li.list
        = link_to edit_user_path(current_user) do
          = fa_icon 'cog', class: 'icon'
  / 参加グループ情報  renderで別ファイルで記述
  = render "shared/group"
